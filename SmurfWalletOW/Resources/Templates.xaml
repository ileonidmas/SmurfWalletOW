<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:v="clr-namespace:SmurfWalletOW.View"
                    xmlns:vm="clr-namespace:SmurfWalletOW.ViewModel">
    <DataTemplate DataType="{x:Type vm:DialogYesNoViewModel}">
        <v:DialogYesNo/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogAccountViewModel}">
        <v:DialogAccountView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogAccountUpdateViewModel}">
        <v:DialogAccountView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogEncryptionKeyViewModel}">
        <v:DialogEncryptionKey/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogSettingsViewModel}">
        <v:DialogSettings/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogAboutViewModel}">
        <v:DialogAboutView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DialogNotificationViewModel}">
        <v:DialogNotification/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:RegionViewModel}">
        <v:RegionView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:UpdateViewModel}">
        <v:UpdateView/>
    </DataTemplate>
</ResourceDictionary>