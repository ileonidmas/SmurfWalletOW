<UserControl x:Class="SmurfWalletOW.View.AccountItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:SmurfWalletOW.Model"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=model:Account, IsDesignTimeCreatable=False}" 
            >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label Content="{Binding DisplayName}" />
            <Button Content="Play" Command="{Binding DataContext.PlayCommand,  RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
            <Button Content="Delete" Command="{Binding DataContext.DeleteAccountCommand,  RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource MultiValueConverter}" >
                        <Binding/>
                        <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=Window}"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>
        </StackPanel>
       

    </Grid>
</UserControl>
